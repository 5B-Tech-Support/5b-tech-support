# https://docs.railway.com/guides/config-as-code
# Ensures Next.js runs via npm scripts (not a guessed app.js / server.js entrypoint).
[build]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm run start"
